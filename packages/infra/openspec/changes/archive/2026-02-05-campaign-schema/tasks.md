# 1. Database Schema

- [x] 1.1 Create `packages/db/src/schema/campaigns.ts` with `Campaign` and `Lead` table definitions.
- [x] 1.2 Export new schema from `packages/db/src/schema/index.ts`.

# 2. Verification

- [x] 2.1 Verify types with `tsc` in `packages/db`.
- [x] 2.2 Check OpenSpec artifact consistency with `openspec validate campaign-schema`.
